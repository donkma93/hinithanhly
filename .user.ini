; Increase upload limits for environments that honor per-directory PHP settings
upload_max_filesize = 512M
post_max_size = 512M
memory_limit = 1024M

; Note: Some hosts ignore per-directory .user.ini or require php.ini changes.